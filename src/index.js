import React from 'react'
import ReactDOM from 'react-dom'
import Book from './components/Book'

ReactDOM.render(< Book title='A River Runs Through It'/>, document.getElementById('container'))
// const React = require('react') - this is the browserify way
